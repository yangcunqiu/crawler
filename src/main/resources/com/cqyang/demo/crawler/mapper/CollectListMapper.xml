<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqyang.demo.crawler.mapper.CollectListMapper">

    <insert id="insert">
        insert into collect_list(task_unique_key, task_tag, request_unique_key, unique_key, keyword, data, status) VALUE
            (
                #{taskUniqueKey}, #{taskTag}, #{requestUniqueKey}, #{uniqueKey}, #{keyword}, #{data}, #{status}
                )
    </insert>
</mapper>